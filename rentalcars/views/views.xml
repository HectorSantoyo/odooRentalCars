<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="rentalcars.list">
      <field name="name">rentalcars list</field>
      <field name="model">rentalcars.rentalcars</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="rentalcars.action_window">
      <field name="name">rentalcars window</field>
      <field name="res_model">rentalcars.rentalcars</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="rentalcars.action_server">
      <field name="name">rentalcars server</field>
      <field name="model_id" ref="model_rentalcars_rentalcars"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="rentalcars" id="rentalcars.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="rentalcars.menu_1" parent="rentalcars.menu_root"/>
    <menuitem name="Menu 2" id="rentalcars.menu_2" parent="rentalcars.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="rentalcars.menu_1_list" parent="rentalcars.menu_1"
              action="rentalcars.action_window"/>
    <menuitem name="Server to list" id="rentalcars" parent="rentalcars.menu_2"
              action="rentalcars.action_server"/>
    -->
  </data>
</odoo>